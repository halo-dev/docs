`OperationItem<T>` 由 `@halo-dev/ui-shared` 提供，请直接使用当前依赖中的类型。`priority` 和使用 `markRaw` 包装的 `component` 为必需字段；`props`、`action`、`label`、`hidden`、`permissions` 和递归的 `children` 为可选字段。

`priority` 越大，操作项的显示位置越靠前。
